#include<bits/stdc++.h>
using namespace std;
typedef long long ll;
const int N = 1e7;
int sz[N];
struct{
	int to, last;
};

void dfs(int i){
	
	if(3*i-1 < )
	dfs(3*i-1);
	dfs(3*i);
	dfs(3*i+1);
	
	
}
int main(){
	

	return 0;
}
